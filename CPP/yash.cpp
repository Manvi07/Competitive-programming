#include <bits/stdc++.h>
#define f(i,n) for(int i=0;i<n;i++)
using namespace std;
int main(int argc, char const *argv[]) {
  int n,d,x;
  cin>>n>>d;
  vector <int>p;
  f(i,n)
  {int v;cin>>v;p.push_back(v);}

  return 0;
}
