#include <bits/stdc++.h>
using namespace std;
struct node {
  int data;int key;
  struct node* next;
};

void insert(struct node *head,int val)
{

}
void print(struct node* head) {
  while(head!=NULL)
  {cout<<head->data;
  head=head->next;}
}
int main(int argc, char const *argv[])
{

  return 0;
}
