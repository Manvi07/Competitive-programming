#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[]) {
  int a, n, k;
  cin >> n >> k;
  vector<int>s;
  for(int i=0; i<n; i++)
  {
    cin >> a;
    s.push_back(a);
  }
  return 0;
}
